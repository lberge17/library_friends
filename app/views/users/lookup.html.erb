<%= render 'search' %>

<hr>

<div class="home"> 

    <div class="text-left user-lookup-list">
        <% if @users %>
            <% @users.each do |user| %>
                <%= link_to "#{user.name} - #{user.email}", user_path(user) %><br>
            <% end %>
        <% end %>
    </div>

</div>