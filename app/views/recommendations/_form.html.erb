<%= form_for [@user, @recommendation] do |f| %>
    <%= f.label :comment %><br>
    <%= f.text_area :comment %><br>
    <%= f.label :book_id %>
    <!-- This is going to change to a select field from the users library -->
    <%= f.number_field :book_id %>
    <%= f.submit %>
<% end %>